# Core numerics
numpy>=2.0
scipy>=1.13
pandas>=2.2

# Plotting (kaleido is needed to export static PNGs for the README,
# because GitHub does not render interactive Plotly output in notebooks)
plotly>=6.0
kaleido>=0.2

# Interactive Brokers API client.
# ib_async is the maintained community successor to ib_insync.
ib_async>=2.0

# Notebooks
ipykernel>=6.29

# Tests
pytest>=8.0
